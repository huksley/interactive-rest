{
  "name": "testresti",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "build": "NODE_ENV=production node src/build",
    "start": "NODE_ENV=production node src/server",
    "predist": "npm run build",
    "dist": "rm -Rf api; rm -Rf dist; mkdir -p pages; mkdir -p dist; cp -R .build/src/api/ pages/api/; cp -R static/* dist; cp -R src/pages/* dist/; cp -R .build/src/pages/* dist/",
    "postdist": "find . ! -path \"./node_modules/*\" ! -path \"./.git/*\"",
    "dev": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "livereload": "^0.9.3",
    "mime-types": "^2.1.35",
    "pusher": "^5.1.1-beta",
    "pusher-js": "^7.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-error-boundary": "^3.1.4",
    "react-router-dom": "^6.4.2",
    "swr": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "nodemon": "^2.0.20"
  }
}
